export interface Highlight {
    url: string;
    serializedRange: string;
  } 

  //defines an interface names Highlight with 2 properties; a url that is a string and a serializedRange that is also a string.